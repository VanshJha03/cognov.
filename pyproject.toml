[project]
name = "cognov"
version = "0.1.0"
description = "Accounting API for Inventory Apps"
dependencies = [
    "fastapi",
    "uvicorn",
    "pydantic",
    "supabase"
]

[tool.vercel]
entrypoint = "api/index.py:app"
